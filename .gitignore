# LibreLane / OpenLane flow output (regenerable, huge)
librelane/runs/
# ACV power-connector working ODBs (regenerable via build_acv_connected.sh, ~115MB)
librelane/_patched/
librelane_rtl2gds_gf180/runs/
**/runs/
stage2_padring/librelane/runs_archive/
stage2_padring/gf180mcu_stage2_final/

# Simulation waveforms & dumps
*.vcd
*.fst
*.vpd

# Archives & large binaries
*.zip
*.tar
*.tar.gz
*.tgz

# Local-only chipathon trackers (removed from the remote 2026-07-05, keep local copies untracked)
GAN_CHIP_*.xlsx

# Common junk
__pycache__/
*.log
CLAUDE.md

# macOS metadata junk
.DS_Store
._*

# LaTeX build intermediates anywhere (paper sources + PDFs are tracked; these regenerate)
*.aux
*.out
*.synctex.gz
*.toc
*.bbl
*.blg

# Nested PDK git clone (separate repo, ~1.2GB, not part of this project's history)
stage2_padring/gf180mcu/

# Vendored caravel-gf180mcu reference clone (~1.1GB of signoff artifacts; reference
# only, kept locally under layout_announcement/ but not in this project's history)
layout_announcement/caravel-gf180mcu-main/

# Claude Code settings (local only, not part of project history)
.claude/

# Gate-level-sim compiled binaries embed the whole routed netlist (~73 MB each,
# over GitHub limits) and recompile in seconds -- see "Post-layout simulation" in docs
sim/results/dla_engine_top_gls_tb.vvp
sim/results/g300_pipeline_gls_tb.vvp

# LaTeX build artifacts (paper sources + PDFs are tracked; intermediates are not)
APIC_Paper/*.aux
APIC_Paper/*.out
APIC_Paper/*.synctex.gz
APIC_Paper/*.toc
APIC_Paper/*.bbl
APIC_Paper/*.blg
